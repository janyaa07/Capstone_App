<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- For Android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"/>

    <!-- For older devices -->
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <!-- Required for device discovery -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>



    <application
        android:allowBackup="true"
        android:label="Aire-Venture"
        android:supportsRtl="true"
        android:theme="@style/Theme.AireVenture">


        <!-- Auth / Login screen -->
        <activity
            android:name=".AuthFiles.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pairing screen -->
        <activity
            android:name=".PairingActivity"
            android:exported="false" />

        <!-- Converter screen -->
        <activity
            android:name=".ConverterActivity"
            android:exported="false" />
        <!-- Home screen -->
        <activity android:name=".HomeActivity" />
        <activity android:name=".ConnectedDevicesActivity"/>



    </application>

</manifest>